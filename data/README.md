# Input Data

This directory is intended to contain raw RNA-seq FASTQ files.

Example expected files:
- sample1_R1.fastq.gz
- sample1_R2.fastq.gz

Raw sequencing data are not included due to size and confidentiality constraints.
